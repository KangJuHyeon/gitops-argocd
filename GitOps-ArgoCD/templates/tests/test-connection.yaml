apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "GitOps-ArgoCD.fullname" . }}-test-connection"
  labels:
    {{- include "GitOps-ArgoCD.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "GitOps-ArgoCD.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
